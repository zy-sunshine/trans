complete django.contrib.message show in page.

complete django-registration and django-profiles function in project.

to finish paragraph insert in admin add toc view.

play django.contrib.auth permission module.

http://djangobook.py3k.cn/chapter07/translate/ is the translate page function view in djangobook site.

TODO Add: add a Paragraph translated to handler with translated para, because orig paragraph should have an unique id.
